name: Failed Test
on:
  workflow_dispatch:
jobs:
  Failed-Test:
    runs-on: ubuntu-latest
    steps:
      - name: echo step
        run: |
          echo "Hello World"
      - name: Try to fail
        run: exit 1
